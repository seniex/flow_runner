# Flow Runner Qt 真实 Windows 环境验收清单

记录测试日期、Windows 版本、Python 版本、显示器数量、DPI 比例、OCR 引擎路径和测试项目路径。每项填写 `PASS`、`FAIL` 或 `BLOCKED`，并附截图或日志路径。

自动化基线（2026-07-13）：`213 passed`，Ruff、格式检查、严格 mypy、`pip check` 和 wheel 构建均通过。此结果不替代下列真实桌面/游戏环境验收。

当前实机环境记录（2026-07-13）：Windows 10 专业版 `10.0.19045`，单显示器，虚拟桌面原点 `(0, 0)`、尺寸 `2560×1440`。独立进程调用 DPI 初始化返回 `per_monitor_v2`。PaddleOCR-json 可执行文件存在；Tesseract 与 `pytesseract` 均未安装。

| 项目 | 操作 | 通过标准 | 结果/证据 |
|---|---|---|---|
| 应用启动 | 在项目虚拟环境运行 `flow-runner` | 主窗口打开，无导入异常 | PASS（2026-07-13：主进程和 Qt 子进程正常启动，窗口可响应并可通过 `WM_CLOSE` 正常退出，无导入异常） |
| QSS | 检查三栏、选中和运行状态 | 样式来自 `base.qss`，控件无局部硬编码 | PASS（2026-07-13：全局 `base.qss` 已加载且源码未发现局部样式；属性面板改为滚动容器，首次窗口矩形 `[184,140,2376,1299]` 完整位于 `2560×1440` 可用桌面内，右侧滚动条可用） |
| 单显示器截图 | 预览 desktop 条件 | 截图尺寸和坐标正确 | PASS（2026-07-13：真实桌面捕获原点 `(0, 0)`、尺寸 `2560×1440`） |
| 真实游戏窗口截图 | 捕获 `window:懒人修仙传2` | 窗口边界、绝对原点和内容正确且非空 | PASS（2026-07-13：前台游戏窗口原点 `(444, 63)`、尺寸 `1671×1251`、RGB 三通道标准差 `[45.72, 49.89, 65.89]`） |
| 后台游戏窗口截图 | 用其它窗口完全遮挡游戏后捕获 `window:background:懒人修仙传2` | 返回游戏内容而非遮挡内容，结束后会话与窗口状态恢复 | PASS（2026-07-13：Chrome 遮挡时，WGC 帧与遮挡前游戏帧平均像素差 `0.037`，与屏幕可见 Chrome 像素差 `40.87`；完整应用组合链路返回 `capture_mode=background`、尺寸 `2576×1416`、原点 `(-8,-8)`；捕获会话正常停止并恢复游戏前台） |
| 多显示器截图 | 在副屏框选目标 | 区域和绝对坐标正确 | BLOCKED（当前系统只检测到 1 个显示器） |
| DPI | 分别使用 100%/125%/150% | 框选与点击坐标一致 | BLOCKED（Per-Monitor V2 初始化 PASS；尚未切换 100%/125%/150% 实测） |
| Tesseract | 配置可用程序与语言包 | 文本、矩形、置信度可见 | BLOCKED（未找到 Tesseract，虚拟环境无 `pytesseract`） |
| PaddleOCR-json | 配置 `PaddleOCR-json_v1.4.1/PaddleOCR-json.exe` | 中文文本和坐标可见，退出后进程结束 | PASS（2026-07-13）：对用户提供的实际桌面/游戏截图识别 148 项；样本“无响应”边界 `[794, 560, 837, 582]`、置信度 `0.999757`；识别前后 Paddle 进程数均为 0 |
| 图片匹配 | 使用已知模板 | 命中坐标与阈值正确 | PASS（2026-07-13：从真实桌面帧取得 `120×80` 已知模板，阈值 `0.99` 时置信度 `1.0`、边界 `[853, 480, 973, 560]`） |
| 像素检测 | 选择固定颜色点 | 容差内命中，容差外不命中 | PASS（2026-07-13：真实桌面点 `(1280, 720)`；颜色差 3、容差 5 时命中，反色、容差 5 时不命中） |
| 区域变化 | 切换游戏画面 | 连续帧变化比例正确 | PASS（2026-07-13：前台游戏窗口中央区域首帧比例 `0`，1 秒后变化比例 `0.060721`，阈值 `0.001` 时命中） |
| 窗口/进程条件 | 查询真实窗口与进程 | 存在项命中并返回真实状态 | PASS（2026-07-13：`懒人修仙传2` 窗口存在且为前台；`explorer.exe` 进程条件命中） |
| 窗口动作 | 激活、最小化、恢复、移动和缩放测试窗口 | 操作命中指定窗口，坐标/尺寸正确且不影响其它窗口 | PASS（2026-07-13：临时 Qt 窗口依次激活、最小化、恢复、移动缩放到 `(320,180,640,420)` 并重新置前；Win32 矩形为 `[320,180,960,600]`） |
| 程序启动 | 分别普通启动和管理员启动测试程序 | 参数、工作路径和权限模式正确，失败时报告明确错误 | BLOCKED（2026-07-13：普通启动、参数和工作目录实机通过，`cmd.exe` 报告路径与配置的 `C:\Users\Administrator\AppData\Local\Temp\flow_runner_cwd_acceptance` 完全一致；管理员启动仍需用户确认 UAC） |
| 鼠标 | 点击、移动、滚轮 | 按配置执行且停止可中断 | PASS（2026-07-13：移动、单击、双击、右击、滚轮、按下/释放和拖拽均产生真实事件；修复后 Runner 在长移动开始 `0.25s` 时停止，鼠标停在 `(409,404)`，再等 `0.8s` 位置不变，结果为 `cancelled`） |
| 键盘 | 单键、组合键、文本 | 按配置执行且停止可中断 | PASS（2026-07-13：单键、组合键、按下/释放和终止清理无残留；`unicode` 在中文输入法环境准确输入“中文测试”，`clipboard` 准确输入“剪贴板模式”并恢复原剪贴板 `FLOW_RUNNER_CLIPBOARD_SENTINEL`；`keys` 保留物理按键/输入法语义） |
| 录制回放 | 保存并回放短脚本 | 事件顺序、速度、最大停顿正确 | PASS（2026-07-13：真实录制得到 click、scroll、key_press、key_release，回放后鼠标回到 `(650,400)`；按住 Ctrl 的回放中途取消后系统 Ctrl 状态为释放） |
| 全局热键 | 启动、暂停、停止、录制 | 非焦点窗口下仍触发且无重复绑定 | BLOCKED（2026-07-13：系统仍运行 `[Running] BgOcrClick Qt - 懒人修仙传2`，直接发送 F6–F9 可能触发旧程序并影响游戏；需先由用户关闭或确认隔离） |
| 跨组路由 | 运行 A1→A2→A3 循环后转 B/C | 路径和计数符合规则 | PASS（2026-07-13：专项运行时测试覆盖 A1→A2→A3 循环计数后转 B/C） |
| 调用返回 | 主流程调用子流程 | 子流程完成后回到下一步骤 | PASS（2026-07-13：专项运行时测试验证子流程返回调用者下一步骤） |
| 并行监控 | 两个只读监控同一目标 | 共享帧，无重复高频截图 | PASS（2026-07-13：专项运行时测试验证并行子流程、共享任务上下文和同 tick 感知缓存） |
| 资源冲突 | 两流程同时点击同一窗口 | 动作串行，旧坐标执行前重检 | PASS（2026-07-13：专项运行时测试验证独占资源串行、等待取消和场景代次变化后的锁内重检） |
| 暂停恢复 | 检测与动作间暂停 | 不启动下一步骤，恢复后继续 | PASS（2026-07-13：专项运行时测试验证条件命中与动作之间暂停，以及步骤间暂停恢复） |
| 停止退出 | 轮询或等待中停止并关闭 | 有界时间内结束，无残留监听/进程 | PASS（2026-07-13：专项运行时测试验证轮询、等待和资源获取取消；真实录制监听正常停止且本次测试无新增进程残留。长输入动作的物理中断缺陷单列在鼠标/键盘项） |
| 配置恢复 | 人为损坏主 JSON | 报告明确错误，备份仍可恢复 | PASS（2026-07-13：损坏 JSON 被明确报告；5 份备份中最新备份成功恢复 `version-5`） |
| 原子保存 | 保存时模拟异常 | 原文件不被半写入文件替换 | PASS（2026-07-13：模拟 `os.replace` 失败，原字节保持一致且无 `.tmp` 残留） |

全部必需项为 `PASS` 后，才能宣告真实环境验收完成。
