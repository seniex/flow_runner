# 旧配置转换报告

转换日期：2026-07-13  
源配置：`config/flow_runner.json`  
目标配置：`project.json`

## 转换结果

- 流程组：4
- 流程：99
- 步骤：159
- 外部文件引用：76，缺失 0
- 能力配置错误：0
- UUID/路由引用错误：0

原流程组和组内 `next_flow` 已转换为稳定 UUID 路由：

- `不思议挂机`：流程 31 完成后返回流程 0；流程 32–34 单独结束。
- `不思议挂机B`：流程 27 完成后返回流程 0；流程 28–30 单独结束。
- `不思议挂机C`：流程 28 完成后返回流程 0；流程 29–31 单独结束。
- `装备背包`：流程 0 完成后结束。

## 兼容映射

- OCR 缩放保留为 `scale`，识别结果坐标会还原为原桌面绝对坐标。
- OCR/图片循环转换为 `UNTIL`，旧版首轮前等待和 `pre_action` 放入每轮前动作。
- `max_retries` 按旧版实际的 `max_retries + 1` 次检测转换。
- OCR 轮询的 `on_timeout=continue` 转换为 `TIMEOUT` 路由到下一步骤或下一流程。
- 识别坐标点击使用 `$result.primary.position`；绝对坐标、偏移、左右键、双击、次数和前置等待均保留。每次点击恢复旧版约 `15ms` 的预移动和 `15ms` 稳定等待，避免游戏忽略瞬移后立即发生的点击。
- 旧版点击抖动转换为 `jitter_pixels=3`。
- 旧版滚轮倍率转换为 Windows 原始滚轮增量（`120 × delta_mul`）。
- Python/Pythonw 脚本通过当前项目虚拟环境解释器隐藏启动，工作目录保持为脚本目录，避免控制台窗口遮挡游戏或影响画面检测。
- 新项目整体以管理员权限运行时，子 Python 脚本直接继承权限，不重复触发 UAC。
- 录制回放保留速度、最大停顿和 `jitter_ms`；`max_gap=0` 表示不限制原始停顿，时间抖动围绕绝对事件时间计算，不会逐事件累计拖慢。

## 录制文件

旧格式的首事件时间已归零；鼠标按下/释放对折叠为新格式原子点击。旧播放引擎本来就忽略水平滚轮，因此转换器也只保留垂直滚轮。

| 文件 | 旧事件 | 新事件 |
|---|---:|---:|
| `亮屏.json` | 9 | 8 |
| `基本卡片.json` | 1022 | 997 |
| `退出游戏.json` | 263 | 259 |

新文件位于 `recordings/legacy/`。

## 项目设置

- OCR：PaddleOCR-json v1.4.1
- 捕获目标：`desktop`，前台桌面模式，保持旧版绝对坐标语义
- F11：启动选中的流程
- F12：停止
- F6：开始/停止录制
- 暂停热键：禁用（与旧配置一致）

项目启动后不会自动执行。必须先在左侧选择要运行的流程，再点击“启动”或按 F11。
